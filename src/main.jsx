import React from "react";
import { createRoot } from "react-dom/client";
import Salvo from "../Salvo.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Salvo />
  </React.StrictMode>
);
