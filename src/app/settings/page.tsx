import Placeholder from "@/components/Placeholder";

export default function SettingsPage() {
  return (
    <Placeholder
      title="Settings"
      phase="Phase 4–5"
      blurb="GHL connection (Private Integration / OAuth), org settings, and the suppression list for opt-outs and already-contacted."
    />
  );
}
