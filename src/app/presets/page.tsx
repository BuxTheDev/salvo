import Placeholder from "@/components/Placeholder";

export default function PresetsPage() {
  return (
    <Placeholder
      title="Presets"
      phase="Phase 5"
      blurb="Saved underwriting profiles (e.g. Phoenix Creative, Vegas Cash) scoped per organization."
    />
  );
}
